ЛР1 — Ввод/вывод и форматирование
задание 1
-Строка 1: Запрашивает у пользователя имя и сохраняет его в переменную name.
-Строка 2: Запрашивает возраст, преобразует ввод в целое число (int) и сохраняет в age.
-Строка 3: Выводит приветствие, используя f-строку, и вычисляет возраст через год (age+1).
<img width="1033" height="145" alt="Снимок экрана 2026-09-16 193353" src="https://github.com/user-attachments/assets/d1b6fa91-aee4-4ff8-9bd6-0d643ca86c9b" />

задание 2
-Строки 1-2: Считывают два числа, заменяют запятые на точки (для корректного ввода дробей) и преобразуют их в вещественные числа (float).
-Строка 3: Выводит сумму и среднее арифметическое ((a+b)/2), округляя результат до двух знаков после запятой (:.2f).
<img width="670" height="132" alt="Снимок экрана 2026-09-16 194002" src="https://github.com/user-attachments/assets/22fdc116-2ba5-4942-9836-bb1bee094c48" />

задание 3
-Строки 1-3: Считывают цену, процент скидки и процент НДС, преобразуя их в дробные числа.
-Строки 4-6: Вычисляют базу после скидки (base), сумму НДС (vat_amount) и итоговую стоимость (total).
-Строки 7-9: Выводят результаты расчетов, округляя до двух знаков после запятой.
<img width="647" height="314" alt="Снимок экрана 2026-09-16 194015" src="https://github.com/user-attachments/assets/20075736-874e-4509-aa64-13bdbd131a2b" />

задание 4
-Строка 1: Запрашивает количество минут, преобразует в целое число (int).
-Строка 2: Выводит время в формате ЧЧ:ММ. Часы вычисляются целочисленным делением (//), минуты — остатком от деления (%), при этом минуты форматируются как двузначное число (:02d).
<img width="536" height="146" alt="Снимок экрана 2026-09-16 194022" src="https://github.com/user-attachments/assets/eb672dbe-4a04-4f0c-a096-e8e8c3911f6a" />

задание 5
-Строка 1: Запрашивает ФИО, разбивает строку по пробелам в список fio.
-Строка 2: Выводит инициалы, обращаясь к первым символам ([0]) каждого из трех элементов списка.
-Строка 3: Выводит общую длину всех трех частей ФИО (без учета пробелов).
<img width="696" height="155" alt="Снимок экрана 2026-09-16 194028" src="https://github.com/user-attachments/assets/96c0e3aa-ca74-481a-b5fa-f68436a41961" />
задание 6
-Строка 1: Запрашивает количество студентов (n) и преобразует ввод в целое число.
-Строка 2: Инициализирует счетчики для отличников (och) и заочников (zaoch), начиная с нуля.
-Строка 3: Запускает цикл, который выполнится n раз (для каждого студента).
-Строка 4: Считывает строку с данными студента и разбивает её по пробелам на четыре переменные: имя, фамилия, возраст, форма обучения.
-Строка 5: Если форма обучения равна 'True' (отличник), увеличивает счетчик och на 1.
-Строка 6: Иначе (если 'False' — заочник) увеличивает счетчик zaoch на 1.
-Строка 7: Выводит итоговое количество отличников и заочников через пробел.
<img width="749" height="321" alt="Снимок экрана 2026-09-22 111241" src="https://github.com/user-attachments/assets/ecc901cb-885e-4e53-a086-37fb022d711a" />
задание 7
-Строка 1: Запрашивает у пользователя зашифрованную строку.
-Строка 3: Инициализирует переменную nom1 (начало слова) нулем.
-Строки 4-7: Находит индекс первой заглавной буквы в строке, сохраняет его в nom1 и прерывает цикл (это начало оригинального слова).
-Строка 8: Находит индекс первой точки в строке.
-Строка 10: Обрезает строку от первой заглавной буквы до точки (включительно), отбрасывая всё лишнее до и после.
-Строка 12: Заменяет все цифры от 1 до 9 на '0', чтобы унифицировать их для поиска шага.
-Строки 13-16: Ищет шаг шифрования. Проходит по строке и находит индекс i, где символ равен '0', а следующий за ним — не '0'. Этот индекс i и есть искомый шаг (расстояние между буквами).
-Строка 18: Формирует итоговую строку, беря каждый shag-й символ из обрезанной строки (срез с шагом).<img width="671" 
-Строка 20: Выводит расшифрованное слово.
<img width="671" height="607" alt="Снимок экрана 2026-09-22 183752" src="https://github.com/user-attachments/assets/9d4a428a-c4f0-4f2f-895f-0d145e788651" />
